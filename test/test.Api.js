( function () {
	QUnit.module( 'Api' );

	QUnit.test( 'getQueryParameter', function ( assert ) {
		assert( true );
	} );

	QUnit.test( 'getAjaxURL', function ( assert ) {
		assert( true );
	} );
}() );
